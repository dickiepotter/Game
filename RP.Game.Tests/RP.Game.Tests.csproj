<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <AssemblyName>RP.Game.Tests</AssemblyName>
    <RootNamespace>RP.Game.Tests</RootNamespace>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Mirror RP.Math.Tests exactly so the libraries' test setups are siblings: the classic VSTest
       runner (Microsoft.NET.Test.Sdk) keeps `dotnet test` working without the .NET 10 SDK's
       Testing-Platform opt-in, MSTest is the framework, FluentAssertions the assertion style. -->
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="MSTest.TestAdapter" Version="3.6.0" />
    <PackageReference Include="MSTest.TestFramework" Version="3.6.0" />
    <PackageReference Include="FluentAssertions" Version="6.12.0" />
    <PackageReference Include="coverlet.collector" Version="6.0.2">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>

  <!-- One test project covers both halves of the split. RP.Game is named explicitly even though it
       would arrive transitively, because every test in here exercises an engine type: not one of
       them touches Silk.NET, Vulkan or OpenAL, which is precisely why the seam was safe to cut. -->
  <ItemGroup>
    <ProjectReference Include="..\RP.Game\RP.Game.csproj" />
    <ProjectReference Include="..\RP.Game.Silk\RP.Game.Silk.csproj" />
  </ItemGroup>

</Project>
